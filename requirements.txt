# Upper bound added deliberately: mcp 2.0.0 removed `mcp.server.fastmcp`, which is what this
# server is built on, so the unbounded `>=1.2.0` resolves to a release that cannot import.
mcp>=1.2.0, <2.0.0
httpx>=0.27.0
